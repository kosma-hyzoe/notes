# Tmux

## Basics

* `new -s SESSION_NAME`
* `attach -t TARGET_NAME`
* `attach` - attach the last session?

`detach`
`list-sessions`

* `,` - rename
* c - create session
* x/ctrl+d(prefix-less) - kill pane
* z - zoom/unzoom
