* image -> application, services and os layer, snapshot with packaged binaries
  * can be found on docker hub
  * `image: some-image:some-tag`
* container -> process based on said image
* docker pull/run <image:tag>
